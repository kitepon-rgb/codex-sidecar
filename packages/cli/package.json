{
  "name": "@codex-sidecar/cli",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "bin": {
    "codex-sidecar": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@codex-sidecar/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
