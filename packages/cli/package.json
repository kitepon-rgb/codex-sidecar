{
  "name": "codex-sidecar-cli",
  "version": "0.3.0",
  "private": false,
  "license": "MIT",
  "type": "module",
  "bin": {
    "codex-sidecar": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "codex-sidecar-core": "^0.3.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
