{
  "name": "codex-sidecar-cli",
  "version": "0.3.8",
  "private": false,
  "license": "MIT",
  "type": "module",
  "bin": {
    "codex-sidecar": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json && node --test dist/index.test.js"
  },
  "dependencies": {
    "codex-sidecar-core": "workspace:0.3.8"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
